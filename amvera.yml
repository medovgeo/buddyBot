version: null
meta:
  environment: docker
  toolchain:
    name: docker
    version: latest
build:
  dockerfile: Dockerfile
  skip: false
run:
  command: java -jar app.jar
  persistenceMount: /data
  containerPort: 80
  servicePort: 80
